#App Running on local system

If Node.js is installed on the system :-

1.Open the Node.js command prompt

2.Go to the path where project has been extracted from zip.

3.Run the command- npm start

Once the server is started open on the browser - http://localhost:3000

Home page of the app opens